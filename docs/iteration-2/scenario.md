# 迭代2 业务场景理解

在迭代1用户认证的基础上，实现课程管理模块：

- 教师可以创建课程、编辑自己的课程、删除自己的课程。
- 学生可以浏览课程列表、按名称搜索课程、查看课程详情。
- 课程信息包括：名称、描述、学分、容量、上课时间、授课教师（自动关联当前登录教师）。

**本迭代不实现**：学生选课、退课、成绩录入（留给迭代3和4）。

**依赖**：
- 所有功能要求用户已登录（依赖迭代1认证）。
- 课程的“授课教师”关联到 `User` 实体。
- 权限控制依赖角色：教师才能创建/编辑/删除课程。

参与人：刘书恒